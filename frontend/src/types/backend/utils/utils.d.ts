export declare function getFinalPrice(price: number, discount: number): number;
export declare function getDiscountPercentage(originalPrice: number, discount: number): string | number;
export declare function getReducePrice(originalPrice: number, discount: number): number;
